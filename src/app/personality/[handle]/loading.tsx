import LoadingCard from "@/components/LoadingCard";

export default function Loading() {
  return (
    <LoadingCard
      title="Reading the posts..."
      subtitle="AI is analyzing the recent public posts to find the archetype."
    />
  );
}
