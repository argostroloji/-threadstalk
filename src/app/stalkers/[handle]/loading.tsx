import LoadingCard from "@/components/LoadingCard";

export default function Loading() {
  return (
    <LoadingCard
      title="Building your circle..."
      subtitle="Scanning public replies, mentions and quotes from the last 90 days."
    />
  );
}
